#include "network_audio.h"

#include "esp_log.h"

static const char *TAG = "network_audio";

esp_err_t network_audio_init(void)
{
    ESP_LOGI(TAG, "network audio placeholder ready for stage 5");
    return ESP_OK;
}
